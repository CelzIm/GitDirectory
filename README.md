\# Шпаргалка по Git для начинающих 🚀



Бро, если ты только вкатываешься в Java и не знаешь, как не потерять свой код — Git твой лучший друг. Вот база, которую я сам освоил.



\## С чего начать (Инициализация)

Чтобы папка превратилась в репозиторий, заходи в неё через Git Bash и пиши:

\* `git init` — оживляем проект (создается скрытая папка .git).



\## Как сохранить прогресс (Коммиты)

1\. `git status` — проверяем, что там изменилось (красное — не готово, зеленое — в индексе).

2\. `git add .` — закидываем все изменения в "корзину" перед сохранением.

3\. `git commit -m "Твой коммент"` — фиксируем изменения. \*\*Флаг -m обязателен!\*\*



\## Как подружиться с GitHub

1\. Создаешь пустой репозиторий на сайте.

2\. `git remote add origin ССЫЛКА\_НА\_РЕПО` — знакомим твой комп с облаком.

3\. `git push -u origin master` — отправляем файлы в первый раз (дальше можно просто `git push`).



\## Если накосячил

\* `rm -rf имя\_папки` — если надо снести папку под корень в Git Bash (будь осторожен!).

\* `git log` — посмотреть историю своих подвигов (коммитов).



Git — это не страшно, если под рукой есть такая шпаргалка. Удачи в кодинге!

